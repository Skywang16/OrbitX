// This module is currently empty but reserved for future workspace-related components.
// It was kept to maintain the module structure.

use std::sync::Arc;

use crate::agent::prompt::components::types::ComponentDefinition;

pub fn definitions() -> Vec<Arc<dyn ComponentDefinition>> {
    vec![]
}
